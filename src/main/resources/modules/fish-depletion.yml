# Whether the fish depletion module is enabled.
enabled: true

# Whether players should receive an action bar message when fishing is blocked.
notify-player: true

# Maximum fish resource stored in each chunk.
max-fish: 64

# Amount of fish restored each regeneration cycle.
regen-amount: 2

# Seconds between each fish regeneration cycle.
regen-interval-seconds: 120

cleanup:
  # Whether full fish pools should be removed from memory and storage cleanup.
  unload-when-full: true

  # Seconds before an inactive chunk record can be removed.
  inactive-ttl-seconds: 1800

storage:
  # Size of each region in chunks.
  # Larger values create fewer files, but each file contains more chunk data.
  region-size-chunks: 32

  # Whether empty saved regions should be unloaded from memory after saving.
  unload-empty-regions-after-save: true

  # Maximum number of regions kept in memory.
  # Set to 0 to disable this limit.
  max-loaded-regions: 128

  # Whether clean inactive regions should be unloaded from memory.
  unload-inactive-loaded-regions: true

  # Seconds before a clean loaded region is considered inactive.
  loaded-region-inactive-ttl-seconds: 900

  # Maximum number of regions that can be unloaded during one cleanup pass.
  max-region-unloads-per-cleanup: 8